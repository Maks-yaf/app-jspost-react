import React from 'react';

const Error = () => {
    return (
        <div>
            <h1 style={{color: 'red'}}>
                Error...you link to a non-existent page
            </h1>
        </div>
    );
};

export default Error;